package Paranoia::Names;

use Modern::Perl    '2012';
use Moose;
use Log::Log4perl   ':easy';

sub person {
	# Generate the name of a suitable person
	my $region  = shift;

}

__PACKAGE__->meta->make_immutable;
